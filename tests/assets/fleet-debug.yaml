spec:
  template:
    spec:
      containers:
        - name: fleet-controller
          image: ttl.sh/fleet:dev
          command:
            - fleetcontroller
            - --debug
            - --debug-level
            - "0"
